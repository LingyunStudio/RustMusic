# RustMusic：我用 Rust 亲手写了一个桌面音乐播放器

> 一个本地曲库 + 网易云/QQ 音乐在线曲库 + 逐字歌词 + 桌面歌词 + 10 段均衡器的 Windows 桌面播放器，
> 安装包只有 6.7 MB，纯 Rust 解码引擎，无 FFmpeg 依赖。
> 开源地址：https://github.com/LingyunStudio/RustMusic （Apache-2.0）

## 为什么要自己写一个播放器

市面上的音乐播放器大致分两类：一类是重量级的"音乐平台客户端"，功能齐全但臃肿，广告、直播、社交一个不少；另一类是轻量的本地播放器，界面停留在上个时代，在线能力又几乎为零。

我想要的其实很简单：**一个好看的、够快的、把本地曲库和在线曲库放在一起的播放器**——想听本地 FLAC 就拖进去，想搜新歌就直接搜，歌词要逐字点亮，界面要暗色玻璃质感，而且它应该是个"小而快"的原生应用，而不是一个套着浏览器壳的网页。

于是就有了 RustMusic。技术栈选了 **Tauri 2 + Rust + React**：后端是纯 Rust，前端是 React 渲染的界面。同样一套架构，未来也可以低成本扩展到 macOS 和 Linux。

## 功能总览

| 模块 | 能力 |
| --- | --- |
| 播放引擎 | MP3 / FLAC / WAV / OGG / M4A / AAC 全格式，纯 Rust 解码，倍速 0.5x–2x |
| 音源 | 本地媒体库、网易云在线曲库、QQ 音乐在线曲库、自定义直链音源 |
| 歌词 | `.lrc` 与内嵌歌词、QQ QRC **逐字歌词**解密、全屏歌词页 |
| 桌面歌词 | 独立透明悬浮窗、逐字卡拉 OK 染色、锁定穿透、自定义配色 |
| 均衡器 | 10 段实时 DSP，内置流行 / 摇滚 / 古典 / 爵士 / 电子 / 人声 6 种预设 |
| 播放管理 | 播放队列（下一首播放 / 拖动排序）、播放列表、我喜欢、最近播放 |
| 桌面集成 | SMTC 系统媒体控制、系统托盘、无边框窗口、拖拽导入 |
| 输出设备 | 手动指定输出设备，或自动跟随系统默认设备（耳机插拔即切） |
| 界面 | 暗色玻璃拟态、封面主色氛围光、15 种主题强调色、深浅色双模式 |

## 功能详解

### 本地媒体库：扫得快，更得省

本地曲库支持添加多个文件夹，扫描是 **rayon 并行**的——多核 CPU 同时解析不同文件，几千首的曲库首扫也就几秒钟。

真正影响日常体验的是增量更新：每个文件记录了修改时间和大小，二次扫描时**未变化的文件直接跳过**，不做重复解析。文件被删了或移走了，曲库里的失效条目也会自动清理。标签解析基于 lofty，支持 ID3v2 / FLAC / MP4 等格式；遇到没有标签的文件，会按"艺术家 - 标题"的文件名模式智能回退，不至于显示一堆乱码文件名。

内嵌封面会自动提取并压缩缓存，显示时直接读缓存；没有封面的歌曲则用一张动态渐变占位图兜底，视觉上不突兀。

### 在线曲库：搜索即点即播

内置了网易云和 QQ 音乐两个在线曲库，两者共用同一套在线界面：搜索 → 点歌 → 自动下载缓存 → 播放。

登录采用扫码方式，登录凭证只存在本机。**播放始终遵循账号自身的权益**：免费曲目匿名可播，会员曲目登录后按会员权益获取播放链接。这里没有任何绕过会员或版权限制的设计——我做的是客户端体验，不是破解工具。

自定义在线音源则允许添加任意 http/https 音频直链，同样自动下载缓存，适合播客、电台录制这类自有内容的场景。

### 歌词系统：从整行到逐字

常规歌词支持 `.lrc` 同名文件与音频内嵌歌词两种来源，全屏歌词页逐行滚动、当前行高亮、点击任意一行跳转到对应位置。

比较有意思是 **QQ 音乐的 QRC 逐字歌词**。QRC 是一份 Triple-DES 加密、zlib 压缩的 XML，而且 QQ 音乐用的 DES 是非标准置换序——解出来之后每行是 `[行起始,行时长]字(字起始,字时长)...` 的毫秒级逐字时间轴。我把它解密并转换成带 `<timestamp>` 标记的逐字增强 LRC，播放时歌词就能像卡拉 OK 一样逐字点亮，而不是整行翻页。

### 桌面歌词：悬浮在一切之上

桌面歌词是一个**独立的透明窗口**：html/body 全透明，文字直接悬浮在桌面上，永远置顶。支持逐字 / 逐行两种着色，已唱、未唱、下一句三个状态的颜色都可以在设置里自定义。平时可以拖动调整位置，也可以一键锁定——锁定后窗口对鼠标完全穿透，不影响下面的任何操作，歌词安安静静地飘在最上层。

### 10 段均衡器：实时 DSP

均衡器不是摆设，是真正的实时 DSP：10 个频段用 RBJ 公式的双二阶滤波器（biquad）实现，拖动滑块立即生效，无需重启播放。内置流行、摇滚、古典、爵士、电子、人声六种预设，也可以自己调一套。

### 输出设备：插上耳机的那一瞬间自动切换

设置里可以手动指定输出设备，也可以保持"跟随系统"。跟随模式下有一个后台线程监视系统默认输出设备的变化——拔掉耳机，播放自动切回扬声器；插回耳机，又自动切回来，不需要打开设置面板手动选。

### 系统集成：像一个真正的 Windows 应用

- **SMTC（系统媒体传输控制）**：键盘媒体键直接控制播放，系统媒体浮窗（Win+K 那个面板）显示曲名、艺术家、封面和进度
- **系统托盘**：托盘菜单播放 / 暂停 / 切歌，左键点击回到主界面
- **无边框自绘标题栏**，直接把文件或文件夹拖进窗口即可导入资料库

## 界面与设计

RustMusic 的界面是暗色玻璃拟态风格：播放栏、侧栏、弹窗都是磨砂玻璃层，配合细描边和柔和高光。一个我自己很喜欢的小设计是**封面主色氛围光**——程序会实时提取当前封面图片的主色，把这抹颜色化作整个界面的环境光晕，换一首歌，整个界面的"灯光"就跟着换一种颜色。听感之外，视觉也在跟着音乐呼吸。

主题系统提供 15 种强调色（默认琥珀色），深浅色两种模式全局可切。应用图标是一组彩虹六色的声波条——中间最高、两侧渐短，呼应内置的 10 段均衡器，在中性暗色贴片上配一条彩虹，小到 16px 的托盘图标依然清晰可辨。

## 技术架构

```
├── index.html               # 主窗口入口
├── desktop-lyrics.html      # 桌面歌词窗口入口（Vite 多页构建）
├── src/                     # React 前端
│   ├── components/          # 播放栏、歌词页、队列、通用组件
│   ├── views/               # 资料库 / 播放列表 / 在线曲库 / 音源 / 设置
│   ├── desktop-lyrics/      # 桌面歌词窗口前端
│   ├── hooks/               # 通用 hooks（如播放队列的拖拽排序）
│   ├── store.ts             # Zustand 状态（队列与播放逻辑在前端编排）
│   └── api.ts               # Tauri invoke / 事件封装
├── installer/               # Inno Setup 打包脚本 + 一键打包脚本
└── src-tauri/               # Rust 后端
    ├── engine.rs            # 音频引擎：rodio、解码、在线源下载缓存
    ├── eq.rs                # 10 段均衡器（biquad）
    ├── library.rs           # 媒体库扫描（rayon 并行）、标签与封面
    ├── lyrics.rs            # LRC 解析
    ├── db.rs                # SQLite 持久化
    ├── netease.rs           # 网易云接口客户端（weapi 加密、扫码登录）
    ├── qq.rs                # QQ 音乐接口客户端
    ├── qrc.rs               # QRC 逐字歌词解密
    ├── smtc.rs              # Windows 系统媒体控制
    └── commands.rs          # Tauri 命令层
```

### 为什么是 Tauri 而不是 Electron

同样的"Web 界面 + 原生后端"，Electron 背着一份完整的 Chromium，空壳就要一百多 MB；Tauri 用系统自带的 WebView2 渲染界面，Rust 只负责干重活。RustMusic 的完整安装包只有 **6.7 MB**——因为前端资源在构建时直接内嵌进了可执行文件，安装包里就一个 exe，没有散落的资源目录。

音频解码全部走 **symphonia 纯 Rust 解码器**，不依赖 FFmpeg：交叉编译少一个大坑，安全性审计也简单得多。数据库是编译进二进制的 SQLite（rusqlite bundled），HTTP 用轻量的 ureq，整体是一个"静态链接为主、拷贝即用"的后端。

### 前后端怎么协作

Tauri 的模型是"命令 + 事件"：前端通过 `invoke` 调用 Rust 的命令层（搜索、取播放链接、扫描曲库、均衡器……），Rust 通过事件把播放进度、歌词行、下载进度推回前端。播放队列与播放编排逻辑放在前端的 Zustand store 里——这样切歌、预加载、模式切换这些"快速且频繁"的逻辑不用跨进程往返，Rust 侧专注做音频输出和 IO。

桌面歌词则是第二个 WebView 窗口，与主窗口通过事件同步当前歌词行，两个窗口之间没有共享状态的负担。

### 一点工程细节

- **图标流水线**：图标源文件是一个 SVG（彩虹声波条），用 resvg 渲染出 1024px 母图和各小尺寸，再由 `tauri icon` 生成 Windows / macOS / Linux / Android / iOS 全平台图标，脚本里还带了一段渲染像素的程序化质检
- **打包**：Windows 安装包用 Inno Setup，一键脚本从构建到出包一条命令完成；安装器会检测 WebView2，缺失时引导去官方下载
- **构建优化**：release 用 LTO + 单编译单元 + 符号剥离，二进制更小更慢生成——桌面应用不在意这几分钟编译时间，在意的是每一次启动和每一 MB 体积

## 版权与定位

需要认真说明的一点：RustMusic 的网易云 / QQ 音乐集成，**只是以用户自己的账号访问平台、按账号既有权益播放**——免费曲目匿名可播，会员曲目登录后按会员权益获取链接。它不包含、也不会添加任何绕过会员或版权限制的功能。自定义在线音源需要用户自行提供有权使用的直链。请支持正版。

## 下载与源码

- **下载安装包**：https://github.com/LingyunStudio/RustMusic/releases （下载 `RustMusic_x.y.z_x64-setup.exe`，双击安装；安装包未签名，SmartScreen 拦截时选"更多信息 → 仍要运行"）
- **源码**：https://github.com/LingyunStudio/RustMusic （Apache-2.0，其中 QRC 解密模块来自开源项目 navidrome-lyrics-plugin，文件头部保留了原始声明）
- **构建**：`npm install` → `npm run tauri dev` 开发调试，`installer/build.ps1` 一键出安装包

Roadmap 上还排着 HLS 流媒体、歌词翻译 / 双语歌词、音频转码与标签批量编辑、macOS / Linux 构建。

如果你也想要一个"轻、快、好看、本地与在线兼顾"的播放器，欢迎试用和提 issue；如果对 Tauri + Rust 的桌面开发感兴趣，源码里的均衡器 DSP、QRC 解密、增量曲库扫描这些模块，或许也值得一看。
